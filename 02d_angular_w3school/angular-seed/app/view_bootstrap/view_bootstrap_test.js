'use strict';

describe('phApp.view_bootstrap module', function() {

  beforeEach(module('phApp.view_bootstrap'));

  describe('view_bootstrap controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewBootstrapCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});