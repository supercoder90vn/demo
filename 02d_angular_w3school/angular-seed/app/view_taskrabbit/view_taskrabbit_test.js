'use strict';

describe('phApp.view_taskrabbit module', function() {

  beforeEach(module('phApp.view_taskrabbit'));

  describe('view_taskrabbit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view_taskrabbitCtrl = $controller('ViewTaskRabbitCtrl');
      expect(view_taskrabbitCtrl).toBeDefined();
    }));

  });
});